# An empty diagram

The fence names a diagram type and then says nothing. mermaid renders this
without complaint, as a blank 16x16 canvas, so only an explicit size check
catches it.

```mermaid
flowchart TD
```
